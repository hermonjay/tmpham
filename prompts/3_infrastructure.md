```
Implement infrastructure only.

Use:

Docker
Docker Compose
uv
Requirements:

Services:

analytics-app
metabase
Persist:

DuckDB database
Metabase metadata
Create:

Dockerfile
docker-compose.yml
.env.example

Commands:

docker compose up

must start successfully.

Use healthchecks.

Optimize Docker layer caching.

Output all files.
```
